import { DocumentUploadDirective } from './document-upload.directive';

describe('DocumentUploadDirective', () => {
  it('should create an instance', () => {
    const directive = new DocumentUploadDirective();
    expect(directive).toBeTruthy();
  });
});
